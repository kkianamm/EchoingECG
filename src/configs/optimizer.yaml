frequency_optimizer: 1
interval: step
optimizer_cfg:
  lr: 0.00003
  weight_decay: 0.01
  betas: [0.9, 0.999]
scheduler_cfg:
  eta_min: 0
warmup_cfg:
  warmup_fraction: 0.03
  warmup_start_factor: 0.001